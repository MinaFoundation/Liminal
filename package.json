{
  "name": "liminal",
  "version": "0.0.0-local",
  "description": "A specification and library for describing programs that blend zero knowledge and runtime environments.",
  "repository": "github:MinaFoundation/Liminal",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    "liminal": {
      "default": "./target/mod.js",
      "types": "./target/mod.d.ts"
    }
  },
  "scripts": {
    "task": "node --enable-source-maps --no-warnings --loader ts-node/esm",
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.12.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
