export {}

// const countRaw = searchParams.get("count")
// assert(typeof countRaw === "string")
// const count = parseInt(countRaw)
// assert(!isNaN(count))
// const keys: string[] = []
// for (let i = 0; i < count; i++) {
//   const sk = PrivateKey.random()
//   keys.push(sk.toBase58())
//   const id = sk.toPublicKey()
//   context.net.addAccount(id, "30000000000")
// }
// return Response.json(keys)
